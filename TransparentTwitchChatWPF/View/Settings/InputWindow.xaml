<Window x:Class="TransparentTwitchChatWPF.View.Settings.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TransparentTwitchChatWPF.View.Settings"
        mc:Ignorable="d"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:ThemeManager.RequestedTheme="Dark"
        ui:TitleBar.ExtendViewIntoTitleBar="True"
        Topmost="True"
        SnapsToDevicePixels="True"
        WindowStyle="ToolWindow"
        Title="Input" WindowStartupLocation="CenterOwner"
        Width="300" Height="150" UseLayoutRounding="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Name="PromptText" Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap" />
        <TextBox Name="ResponseTextBox" Grid.Row="1" MinWidth="250" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Name="OkButton" IsDefault="True" Click="OkButton_Click" MinWidth="60" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" MinWidth="60"/>
        </StackPanel>
    </Grid>
</Window>
